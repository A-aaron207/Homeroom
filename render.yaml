# =============================================================
# LEGACY — Render backend is no longer used.
# Current Architecture:
#   - Frontend: GitHub Pages (auto-deployed via .github/workflows/deploy.yml)
#   - Database: Firebase Firestore (direct client-side SDK)
#   - Source:   GitHub (A-aaron207/Homeroom)
#
# This file is kept for historical reference only.
# =============================================================

# services:
#   - type: web
#     name: homeroom
#     env: python
#     buildCommand: pip install -r requirements.txt
#     startCommand: gunicorn server.app:app
